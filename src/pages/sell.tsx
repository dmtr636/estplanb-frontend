import React from "react";
import Head from "next/head";

const SellPage = () => {
    return (
        <div>
            <Head>
                <title>Продать</title>
            </Head>
            <h1>Продать</h1>
        </div>
    );
};

export default SellPage;
